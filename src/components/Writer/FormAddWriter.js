import React from 'react'
import PropTypes from 'prop-types'

export default function FormAddWriter ({ state, closeModal }) {
  return (
    <h1>oiiii</h1>
  )
}

FormAddWriter.propTypes = {
  // user: PropTypes.oneOfType([
  //   PropTypes.string,
  //   PropTypes.object
  // ]),
  state: PropTypes.array,
  closeModal: PropTypes.func
}
