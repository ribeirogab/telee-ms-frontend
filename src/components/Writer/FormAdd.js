import React from 'react'
import PropTypes from 'prop-types'

export default function FormAdd ({ state, closeModal }) {
  return (
    <h1>oiiii</h1>
  )
}

FormAdd.propTypes = {
  // user: PropTypes.oneOfType([
  //   PropTypes.string,
  //   PropTypes.object
  // ]),
  state: PropTypes.array,
  closeModal: PropTypes.func
}
